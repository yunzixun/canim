import SwiftUI

struct DivideLineView: View {
        var body: some View {
                Spacer().frame(width: UIScreen.main.bounds.width - 32, height: 1).background(Color.secondary)
        }
}

struct DivideLineView_Previews: PreviewProvider {
        static var previews: some View {
                DivideLineView()
        }
}
